# SupplyWatch - Real time product tracker system

**SupplyWatch** is a real-time item availability tracker that monitors product data from the [FakeStore API](https://fakestoreapi.com/), detects price or stock changes, and notifies users of updates. It uses Redis for caching, RDS for persistent storage, and is fully deployable with Terraform on AWS.

## Project is WIP